cubes:
  - name: fellowship_elders
    sql_table: fellowship_elders
    data_source: default

    joins:
      - name: fellowships
        sql: "{CUBE.fellowshipId} = {fellowships.id}"
        relationship: many_to_one

      - name: members
        sql: "{CUBE.memberId} = {members.id}"
        relationship: many_to_one

    dimensions:
      - name: fellowshipId
        sql: "{CUBE}.`fellowshipId`"
        type: string
        primary_key: true

      - name: memberId
        sql: "{CUBE}.`memberId`"
        type: string

      - name: createdAt
        sql: "{CUBE}.`createdAt`"
        type: time

      - name: updatedAt
        sql: "{CUBE}.`updatedAt`"
        type: time

    measures:
      - name: count
        type: count

    pre_aggregations:
      # Pre-aggregation definitions go here.
      # Learn more in the documentation: https://cube.dev/docs/caching/pre-aggregations/getting-started

